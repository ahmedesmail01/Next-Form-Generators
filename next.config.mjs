/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "managethenow.com"], // Add the external domain here
  },
};

export default nextConfig;
